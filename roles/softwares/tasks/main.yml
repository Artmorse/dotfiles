---
#
# dev
#

- name: Install `VSCode` software
  include_role:
    name: softwares/code
    apply: 
      tags: [dev, code]
  when: enable_code | default(False)

- name: Install `dbeaver-ce` and `dbeaver-ee` softwares
  include_role:
    name: softwares/dbeaver
    apply: 
      tags: [dev, dbeaver]
  when: enable_dbeaver | default(False)


- name: Install `postman` software
  include_role:
    name: softwares/postman
    apply: 
      tags: [dev, postman]
  when: enable_postman | default(False)


#
# media
#

- name: Install `photoflare` software
  include_role:
    name: softwares/photoflare
    apply: 
      tags: [media, photoflare]
  when: enable_photoflare | default(False)


- name: Install `ristretto` software
  include_role:
    name: softwares/ristretto
    apply: 
      tags: [media, ristretto]
  when: enable_ristretto | default(False)


- name: Install `vlc` software
  include_role:
    name: softwares/vlc
    apply: 
      tags: [media, vlc]
  when: enable_vlc | default(False)


#
# misc
#

- name: Install `bitwarden` software
  include_role:
    name: softwares/bitwarden
    apply: 
      tags: [misc, bitwarden]
  when: enable_bitwarden | default(False)


- name: Install `deluge` software
  include_role:
    name: softwares/deluge
    apply: 
      tags: [misc, deluge]
  when: enable_deluge | default(False)


- name: Install `filezilla` software
  include_role:
    name: softwares/filezilla
    apply: 
      tags: [misc, filezilla]
  when: enable_filezilla | default(False)


#
# communication
#

- name: Install `discord` software
  include_role:
    name: softwares/discord
    apply: 
      tags: [communication, discord]
  when: enable_discord | default(False)

- name: Install `slack` software
  include_role:
    name: softwares/slack
    apply: 
      tags: [communication, slack]
  when: enable_slack | default(False)
